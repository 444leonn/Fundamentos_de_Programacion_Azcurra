// Implementar una funcion que permita dividir dos numeros enteros por restas sucesivas.

#include <stdio.h>

int division_sucesiva(num) {
    if (numero == 0) {
        
    }
}

int main() {
    int num;

    division_sucesiva(num);
}